rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in users (the shared account) can touch the words.
    match /words/{word} {
      allow read, write: if request.auth != null;
    }
  }
}
