rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Alleen ingelogde gebruikers (het gedeelde account) mogen bij de woorden.
    match /words/{word} {
      allow read, write: if request.auth != null;
    }
  }
}
